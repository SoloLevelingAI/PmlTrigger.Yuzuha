# Yuzuha installation archive

When the user asks to install, update or connect this package, first read
`agent-skills/yuzuha-setup/SKILL.md` completely and follow its referenced
installation guide. This is an installation-only entry point, not permission
to run AVEVA commands or change unrelated client settings.

If the user only asks to inspect or explain the archive, do not install it.
The user must approve the concrete modification preview before execution.
